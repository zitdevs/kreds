---
category: Brand
---

The Kreds logo — the Merge K. Brand identity only: header, footer, favicon,
avatars, docs. **Never place it in front of a number**; that is `KredSymbol`'s
job. Stroke weight thickens automatically as `size` shrinks so the glyph stays
legible down to 16px.
