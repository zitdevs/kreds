import { LeaderboardShowcase } from "@kreds/ui";

/** The product showcase: a live-looking leaderboard, the contribution economy and the event feed. */
export const Default = () => <LeaderboardShowcase />;
